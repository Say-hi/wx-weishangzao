<!--我关联的上家-->
<view>
  <view class="agent" wx:for="{{ agentArr }}">
    <view class="a-top">
      <image class="br" src="{{ item.img }}"></image>
      <text>{{ item.name }}</text>
    </view>
    <text>{{ item.sign }}</text>
  </view>
</view>