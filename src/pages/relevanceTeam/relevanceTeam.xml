<!--我关联的团队-->
<navigator open-type="redirect" url="../supportTeamPage/supportTeamPage?id={{ item.id }}" class="team" wx:for="{{ teamArr }}">
  <image class="br" src="{{ item.image }}"></image>
  <text>{{ item.name }}</text>
</navigator>
<view class="tac" wx:if="{{ teamArr.length == 0 }}">您尚未关联团队哦</view>
<view class="tac" wx:else>--- 到底啦 --</view>